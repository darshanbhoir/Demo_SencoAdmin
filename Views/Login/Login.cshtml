
@{
    ViewBag.Title = "Login";
    Layout = null;
    <link rel="stylesheet" href="~/Content/css/style.css">
    <link rel="stylesheet" href="~/Content/css/bootstrap-grid.min">
    <script src = "~/Content/js/script.js" ></script>
}
<body>
    <div class="login-page-section">
        <div class="container">
            <div class="row">
                <div class="card">
                    <div class="log-in-card">
                        @using (Html.BeginForm("login", "Login", FormMethod.Post))
                        {
                            if (ViewBag.Message != null)
                            {
                                <div class="alert alert-danger" role="alert">
                                    @ViewBag.Message
                                </div>
                            }
                            <div class="card-body">
                                <img src="~/Content/images/synco-logo.jpg" class="img-fluid" alt="">
                                <!-- <h2>SWARNA YOJANA DASHBOARD</h2> -->
                                <p>Please enter your user information.</p>
                            </div>
                            <!-- Form -->
                            <form action="#" enctype="multipart/form-data" id="send_user_register" method="post"
                                  onsubmit="return form_validate_jquery('#send_user_register')" target="_self">
                                @*<form>*@
                                <!-- Username -->
                                <div class="mb-3">
                                    <label for="email" class="form-label">Username</label>
                                    <input name="email" title="email" type="email" validation="email" id="email" class="form-control"
                                           placeholder="Username here">
                                </div>
                                <!-- Password -->
                                <div class="mb-3">
                                    <label for="password" class="form-label">Password</label>
                                    <input type="password" validation="password" id="password" class="form-control" name="password"
                                           placeholder="Password">
                                </div>
                                <!-- Checkbox -->
                                <div class="d-lg-flex justify-content-between align-items-center
                                mb-4">
                                    <div class="form-check custom-checkbox">
                                        <input type="checkbox" class="form-check-input" id="rememberme">
                                        <label class="form-check-label" for="rememberme">
                                            Remember
                                            me
                                        </label>
                                    </div>
                                </div>
                                <div>
                                    <!-- Button -->
                                    <div class="d-grid">
                                        <button type="submit" class="btn-submit">Log In</button>
                                    </div>
                                </div>
                            </form>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
